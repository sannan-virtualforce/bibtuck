.address_form
  %h2 Enter new address
  = simple_form_for @address, :url => [current_user, @address], :remote => true do |f|
    = hidden_field_tag :referrer, request.referrer
    = hidden_field_tag :order_id, @order_id
    = render :partial => 'form', :locals => {:f => f, :is_create_form => true}
