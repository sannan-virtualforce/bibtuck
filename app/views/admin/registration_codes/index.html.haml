%h1 Registration codes
%h2= link_to 'Add new registration code', new_admin_registration_code_path

%table
  %tr
    %th Registration code
    %th Times used/Limit
    %th Created at
    %th

  - @registration_codes.each do |registration_code|
    %tr
      %td= registration_code.code
      %td  #{registration_code.used_count}/#{registration_code.registration_limit.present? ? registration_code.registration_limit : '-'}
      %td= registration_code.created_at
      %td
        = link_to 'Show', [:admin, registration_code]
        = link_to 'Edit', edit_admin_registration_code_path(registration_code)
        = link_to 'Destroy', [:admin, registration_code], :confirm => 'Are you sure?', :method => :delete
