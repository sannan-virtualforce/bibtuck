.send_feedback_popup
  %h2 The good, the bad and the ugly.

  .box
    Seriously, we mean it. Tell us what you like, what you don’t like, and why.
    It will help us give you a better experience.

  = form_for @feedback_message do |f|
    = f.label 'Your email address'
    = f.select :email, { current_user.email => current_user.email, 'anonymous' => nil}

    = f.text_area :body, :placeholder => 'Enter your feedback here.'
    .actions
      = content_tag :button, 'Submit', :type => :submit, :class => [:splash_button, :orange]
